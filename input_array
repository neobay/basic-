bool getUserInput(int arr[], size_t& size) {
	int n;
	cout << "Nhap vao so phan tu cua mang: ";
	cin >> n;
	if (n > 0)
	{
		size = n;
		for (size_t i = 0; i < size; i++)
		{
			cout << "arr[" << i << "] = " << " ";
			cin >> arr[i];	
		}
		return true;
	}
	else
	{
		return false;
	}
}
